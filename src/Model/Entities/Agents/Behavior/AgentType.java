package Model.Entities.Agents.Behavior;

/**
 * This defines types fo each Agent (equivalent to their class). Used to decide which "Action" should be performed from an "ActionList".
 */
public enum AgentType {
    SHEEP,
    WOLF,
    GRASS
}
